%ul.sidebar-nav.d-print-none
  %li= link_to t('link.back'), proposal_path(@solution_proposal.proposal)

- provide(:title, t('solution_proposals.headers.new'))
.page-body
  %h2.text-center.py-3= t('solution_proposals.headers.new')
  = render partial: 'form', locals: { solution_proposal: @solution_proposal, button: 'create' }
