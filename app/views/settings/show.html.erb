<% provide(:title, "Setting DB") %>
<h1>Setting DB</h1>
<br>

<div class="table-responsive">
  <table class="table">
    <tr>
      <th>#</th>
      <th>Name</th>
      <th>#</th>
    </tr>
    <tr>
      <td><%= @setting.id %></td>
      <td><%= @setting.set_name %></td>
      <td><%= link_to 'Edit', edit_setting_path(@setting) %></td>
    </tr>
  </table>
</div>

<%= link_to 'Back', settings_path %>
