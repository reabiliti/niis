- provide(:title, "Редактирование Сертификата #{@certificate.cert_registration_num}")
.link-stuck
  = link_to t('link.back'), :back
%br
%h1
  Редактирование сертификата
  = @certificate.cert_registration_num
= render partial: 'form', locals: { certificate: @certificate }
