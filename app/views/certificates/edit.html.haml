%ul.sidebar-nav.d-print-none
  %li= link_to t('link.back'), certificate_path(@certificate)

- provide(:title, "#{t('certificates.headers.edit')} #{@certificate.cert_registration_num}")
.page-body
  %h2.text-center.py-3 #{t('certificates.headers.edit')} #{@certificate.cert_registration_num}
  = render partial: 'form', locals: { certificate: @certificate, button: 'edit' }
