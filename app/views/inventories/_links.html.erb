<div class="link-stuck no-print">
  <%= link_to t('link.back'), :back %> |
  <%= link_to t('link.edit'), edit_inventory_path(@inventory) %> |
  <%= link_to t('link.destroy'), @inventory, method: :delete,
                                             data: { confirm: "Вы уверены?" }%>
</div>
<br class="no-print">
<br class="no-print">
